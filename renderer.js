//  const { ipcRenderer } = require('electron');

//ipcRenderer.on('code', (event, args) => {
//    let code = document.getElementByTagName('code')[0];
//    event.sender.send(code, args); 
//code.innerHTML; 
//});    
